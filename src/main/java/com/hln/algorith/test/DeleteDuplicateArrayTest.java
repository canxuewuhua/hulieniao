package com.hln.algorith.test;

/**
 * input [0,1,2,2,3,3,4]
 * output 5
 */
public class DeleteDuplicateArrayTest {

    public static void main(String[] args) {
        
    }

    /**
     * 使用双指针 要求是原地删除重复出现的元素 使每个元素只出现一次 返回删除后数组的新长度
     */
    public int deleteArr(int[] arr){
        if (arr == null || arr.length ==0) return 0;
        int p = 0,q = 0;
        for (int i = 0; i < arr.length; i++) {
            
        }
        return 0;
        
    }
}
